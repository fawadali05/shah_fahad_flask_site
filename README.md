# Shah Fahad — Flask Lawyer Website

Multi-page Flask site with admin panel and JSON storage.

## Run locally
```bash
py -m venv venv
venv\Scripts\activate
py -m pip install -r requirements.txt
py app.py
